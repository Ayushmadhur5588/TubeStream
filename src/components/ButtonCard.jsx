const ButtonCard = (props) => {
    const {filterName} = props?.data;
    console.log(props);
  return (
    <div>
      {/*<button className="w-fit font-normal text-sm rounded-lg bg-gray-100 mx-2 py-2">
        {filterName}
  </button>*/}
    </div>
  );
};

export default ButtonCard;
